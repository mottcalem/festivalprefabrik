'use client';
import {Suspense,useState} from 'react';import {useSearchParams} from 'next/navigation';import {CheckCircleIcon,PaperAirplaneIcon} from '@heroicons/react/24/outline';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import data from '@/data/site.en.json';
function Form(){const q=useSearchParams();const[state,setState]=useState('idle');const[error,setError]=useState('');async function submit(e){e.preventDefault();setState('loading');const r=await fetch('/api/quotes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const j=await r.json();if(r.ok)setState('success');else{setError(j.message);setState('error')}}return <main><SiteHeader locale="en"/><section className="quote-page"><div className="wrap quote-layout"><div className="quote-intro"><span className="section-kicker">Free quote</span><h1>Tell us your vision,<br/><em>let’s plan together.</em></h1><p>Complete the form and our specialists will review your needs and contact you.</p></div><div className="quote-card">{state==='success'?<div className="quote-success"><CheckCircleIcon/><h2>Request received.</h2><p>We will review your project and contact you shortly.</p></div>:<form onSubmit={submit}><div className="form-title"><b>Contact details</b><span>* required</span></div><label>Full name *<input name="name" required minLength="2"/></label><div className="form-grid"><label>Phone *<input name="phone" required minLength="10"/></label><label>Email<input name="email" type="email"/></label></div><div className="form-grid"><label>City<input name="city"/></label><label>Model<select name="model" defaultValue={q.get('model')||''}><option value="">Not decided yet</option>{data.projects.map(p=><option key={p.slug}>{p.title}</option>)}</select></label></div><label>Project notes<textarea name="message" rows="5"/></label>{error&&<p className="form-error">{error}</p>}<button className="form-submit" disabled={state==='loading'}>{state==='loading'?'Sending…':<>Send quote request <PaperAirplaneIcon/></>}</button></form>}</div></div></section><SiteFooter locale="en"/></main>}
export default function Quote(){return <Suspense><Form/></Suspense>}
