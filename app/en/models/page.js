import Image from 'next/image';
import { ArrowRightIcon } from '@heroicons/react/24/outline';
import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import data from '@/data/site.en.json';
export const metadata={title:'Prefabricated House Models | Festival Prefabrik',description:'Explore our single-storey prefabricated house models and technical specifications.'};
export default function Models(){return <main><SiteHeader locale="en"/><section className="listing-hero"><div className="wrap"><span className="section-kicker">Festival Prefabrik models</span><h1>Let’s find the right<br/><em>living space for you.</em></h1><p>Explore durable, energy-efficient prefabricated homes designed for different needs.</p></div></section><section className="models-list wrap">{data.projects.map(p=><article key={p.slug}><a href={`/en/models/${p.slug}`}><div className="model-cover"><Image src={p.image} fill alt={p.title} sizes="50vw"/><span>{p.code}</span></div><div className="model-row"><div><h2>{p.title}</h2><p>{p.area} · {p.rooms} · {p.bathrooms} bathroom</p></div><i><ArrowRightIcon/></i></div></a></article>)}</section><SiteFooter locale="en"/></main>}
